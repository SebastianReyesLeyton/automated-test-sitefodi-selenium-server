export const DTO_EMAIL_REQUEST = {
    email: String()
};

export const DTO_LOGIN_REQUEST = {
    email: String(),
    password: String()
};

export const DTO_AVALIABLE_REQUEST = {
    id: String(),
    rol: String()
};